<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team1.project.dto.UsDTO">
    <!-- 댓글 등록 -->
    <insert id="insertCommentUS">
        <selectKey keyProperty="commentid" resultType="int" order="BEFORE">
            select SEQ_COMMENT.nextval as commentid from dual
        </selectKey>
        INSERT INTO US_COMMENT (
            COMMENTID, CONTENTS, WRITER, REGDATE, US_NUM
        ) VALUES (
            #{commentid}, #{content}, #{writer}, SYSDATE, #{us_num}
        )
    </insert>
</mapper>
